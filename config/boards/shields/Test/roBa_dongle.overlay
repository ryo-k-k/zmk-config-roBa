/* ドングル用の設定ファイル */

/ {
    /*
     * ドングルはキーのスキャンや物理レイアウトを持たないため、
     * chosen内の zmk,kscan や zmk,physical-layout は不要です。
     * mock_kscan, default_transform, physical_layout0 ノードもすべて削除します。
     */
};
