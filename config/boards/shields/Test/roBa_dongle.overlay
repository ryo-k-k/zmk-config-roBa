/*
 * roBa Dongle Overlay (Final Version 2)
 */

/ {
    chosen {
        zmk,kscan = &mock_kscan;
    };

    // 必須プロパティである rows と columns を追加した、
    // 完全なダミーのkscanを定義します。
    mock_kscan: mock_kscan {
        compatible = "zmk,kscan-mock";
        rows = <0>;
        columns = <0>;
    };
};
