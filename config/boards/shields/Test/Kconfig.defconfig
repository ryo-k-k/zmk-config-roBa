if SHIELD_ROBA_DONGLE

config ZMK_KEYBOARD_NAME
	default "roBa"

config ZMK_SPLIT_ROLE_CENTRAL
	default y

endif

if SHIELD_CHARYBDIS_DONGLE || SHIELD_ROBA_L || SHIELD_ROBA_R

config ZMK_SPLIT
	default y
	
endif
